// JavaScript that updates the text color of the header and must be imported from the <head> tag
'use strict';
window.addEventListener('load', () => {
  document.querySelector('header').style.color = '#FF0000';
});
